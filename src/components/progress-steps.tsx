import type { Step } from "@/types";

const ProgressSteps = ({
    currentStep,
    steps
}: {
    currentStep: number,
    steps: Step[]
}) => {
    return <div>ProgressSteps</div>
}
export default ProgressSteps;